import prisma from '../db/prisma.js';

/**
 * Приведение почты к единому виду.
 *
 * `Vova@Mail.ru` и `vova@mail.ru` — один и тот же ящик, но для базы это две
 * разные строки, и раньше на них заводились два разных аккаунта. Человек потом
 * не понимает, почему в «своём» проекте пусто.
 *
 * Обрезаем пробелы и опускаем регистр. Доменная часть регистронезависима по
 * стандарту; локальная формально может быть чувствительна, но на практике так
 * не делает ни один почтовый сервис, а путаница от этого — ежедневная.
 */
export function normalizeEmail(email: string): string {
  return email.trim().toLowerCase();
}

/**
 * Найти пользователя по почте без оглядки на регистр.
 *
 * Быстрый путь — точное совпадение с приведённой: так лежат все записи,
 * заведённые после нормализации, и это подавляющее большинство запросов.
 *
 * Медленный путь нужен ради тех, кто зарегистрировался раньше: у них в базе
 * `Vova@Mail.ru`, и поиск по строчной их не найдёт. Без этого правка ради
 * порядка заперла бы живых людей снаружи, а на их адрес завелась бы вторая
 * учётка — ровно та беда, от которой уходим.
 *
 * Сравнение через LOWER() в самой базе, а не подстановкой второго варианта
 * написания: вариантов регистра у адреса из двадцати букв — миллион, и угадать
 * сохранённый перебором нельзя. Запрос сырой, потому что `mode: 'insensitive'`
 * у Prisma есть только для PostgreSQL, а разработка идёт на SQLite — поведение
 * обязано совпадать, иначе беда всплывёт только на проде. LOWER() понимают обе.
 */
export async function findUserByEmail(rawEmail: string) {
  const normalized = normalizeEmail(rawEmail);

  const exact = await prisma.user.findUnique({ where: { email: normalized } });
  if (exact) return exact;

  const rows = await prisma.$queryRaw<{ id: string }[]>`
    SELECT id FROM users WHERE LOWER(email) = ${normalized} LIMIT 1
  `;
  if (!rows[0]) return null;
  return prisma.user.findUnique({ where: { id: rows[0].id } });
}
